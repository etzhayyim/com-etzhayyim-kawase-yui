#!/usr/bin/env nbb
;; nbb replacement for the retired bb.edn `test` task (ADR-2607173000).
;; Run with: nbb --classpath src:test run-murakumo-tests.cljk
(ns run-murakumo-tests
  (:require [clojure.test :as t]
            [kawase-yui.murakumo-test]))

(let [r (t/run-tests 'kawase-yui.murakumo-test)]
  (.exit js/process (if (zero? (+ (:fail r) (:error r))) 0 1)))
