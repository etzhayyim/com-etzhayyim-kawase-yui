{:paths ["src" "test"]
 :tasks
 {test {:doc "Run kawase-yui cljc actor boundary tests."
        :task (do
                (require 'clojure.test
                         'kawase-yui.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'kawase-yui.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
